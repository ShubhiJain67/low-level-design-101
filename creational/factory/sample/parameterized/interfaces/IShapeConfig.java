package creational.factory.sample.parameterized.interfaces;

// Marker interface — each shape that needs construction params gets its own
// config record. Shapes with no params (e.g. Triangle) don't need one.
public interface IShapeConfig {
}
